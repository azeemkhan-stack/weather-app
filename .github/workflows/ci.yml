name: CI

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Check JavaScript syntax
        run: |
          for f in js/*.js; do cp "$f" "${f%.js}.mjs"; done
          node --check js/api.mjs
          node --check js/app.mjs
          node --check js/config.mjs
          node --check js/helpers.mjs
          node --check js/ui.mjs

      - name: Check HTML structure
        run: |
          test -f index.html
          grep -q "</html>" index.html

      - name: Check project folders
        run: |
          test -d js
          test -d css
